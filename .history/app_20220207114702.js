const colors = document.querySelectorAll(".color");
const l = document.querySelectorAll(".img__container");
colors.forEach((color, index) => {
	console.log(color, index);
	// color.addEventListener("click", function () {});
});
